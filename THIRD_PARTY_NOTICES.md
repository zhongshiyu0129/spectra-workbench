# Third-Party Notices

## Runtime dependencies

- `fflate` 0.8.2 — MIT License
- `plotly.js-dist-min` 2.27.0 — MIT License

这些依赖通过 `npm install` 安装，不随本仓库提交的 `node_modules` 一起分发。

## Legacy calculator asset

`assets/光谱计算器（最终版）.html` 是为兼容旧版计算逻辑保留的独立网页资源，其中包含嵌入式第三方前端代码、许可证声明与 ASTM G173 相关数据。本仓库的 MIT License 不会取代其中已有的第三方声明。

在单独再分发、替换或修改该资源及其数据前，请确认原始来源、署名要求和再分发许可。
